import React from "react"
import CandidateList from "./CandidateCard"

function Search(){


    return(
        <div className="search-form">
            
            <form>

            /// can do this by filtering through the candidates and just looking for matching strings. if you want to add an option to search via ID as well be my guest but i really dont think itll be necessary. 
            </form>
            
        </div>
    )
}

export default Search